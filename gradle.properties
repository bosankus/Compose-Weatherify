# Project-wide Gradle settings.
# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.
# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html
# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8
# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
org.gradle.parallel=true
# Reuses task outputs from previous builds (local cache) instead of recompiling/reprocessing —
# big win across module rebuilds and after clean since most module outputs are unchanged.
org.gradle.caching=true
# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app"s APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official
# Stable property name (the org.gradle.unsafe.* prefix was the pre-8.1 incubating name).
org.gradle.configuration-cache=true
# Kotlin Multiplatform: Disable default hierarchy template since network/storage use explicit dependsOn() calls
kotlin.mpp.applyDefaultHierarchyTemplate=false
# app/iosApp now cinterops razorpay-pod via CocoaPods (see app/iosApp/build.gradle.kts).
# Kotlin's Xcode integration refuses to configure when a module both uses CocoaPods and
# is built from an Xcode-invoked context, even though this project's project.yml already
# uses the recommended embedAndSignAppleFrameworkForXcode + CocoaPods "[CP] Embed Pods
# Frameworks" split (not the deprecated all-in-one embedAndSign task) to avoid exactly
# the conflict this check guards against.
kotlin.apple.deprecated.allowUsingEmbedAndSignWithCocoaPodsDependencies=true
# Enabled parallel sync for Gradle 9.4+
org.gradle.tooling.parallel=true
